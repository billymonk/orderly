<div id="<%= dom_id(scheduled_task.task) %>" class="bg-white p-2 rounded-lg shadow-sm mt-2">
  <strong><%= scheduled_task.task.name %></strong>
  <p class="text-sm text-gray-600"><%= scheduled_task.task.description %></p>
</div>
